'use client';

interface SingleTrackPopularityBoxProps {
  popularity: number;
}

const SingleTrackPopularityBox: React.FC<
  SingleTrackPopularityBoxProps
> = ({}) => {
  return <div></div>;
};

export default SingleTrackPopularityBox;
